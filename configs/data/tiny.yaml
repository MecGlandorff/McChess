# Example dataset-builder config. The source PGN is not committed; supply
# a file at `data/raw/tiny.pgn` to use this config (or point `source`
# elsewhere).

source: data/raw/tiny.pgn
source_description: "tiny fixture for smoke-testing the PGN dataset builder"
output_dir: data/processed/tiny
manifest_path: data/manifests/tiny.json
split_ratios: [0.9, 0.05, 0.05]
split_seed: 0
filters: {}
