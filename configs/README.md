# Configs

This directory stores YAML configs for data processing, model training, evaluation, MCTS, and self-play.

Suggested layout:

```text
configs/
  data/
  model/
  train/
  eval/
  selfplay/
```

All reportable experiments should be reproducible from configs.
