[project]
name = "mcchess"
version = "0.1.0"
description = "A compact neural chess research system trained without engine labels."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "python-chess",
    "torch",
    "PyYAML",
    "tqdm",
]

[project.scripts]
mcchess-play = "mcchess.play:main"

[tool.poetry]
packages = [{ include = "mcchess", from = "src" }]

[tool.poetry.group.dev.dependencies]
pytest = "^7.4"
ruff = "^0.15"
mypy = "^1.19"

[tool.poetry.group.notebook.dependencies]
ipykernel = "^7.0"
nbclassic = "^1.3"
ipywidgets = "^8.1"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
addopts = ["--basetemp=.pytest-temp"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
strict = false
